# 0606-C 계획 — 관성·안전 "더 큰 세계" 지식 확장 sub-page

## 요청 (Nick 2026-06-06)
관성·안전 탭의 '더 큰 세계' 카드들을 **별도 sub-page**로 확장.
"꼬리에 꼬리를 무는 지식 확장"(rabbit-hole) — 다른 이야기로 계속 이어지게.
- **묶음1(간단)**: 초대형유조선·화물열차·항만 방충재·단조프레스 → **한 페이지** (+사진)
- **플라이휠**: 제대로 전문적인 설명 + **상징적 차용**(선순환·점진 개선의 'flywheel') 상식 페이지로
- **INS(관성항법)**: Nick도 잘 모름 → 지식 페이지 1개 추가 의미
- **고층빌딩 질량댐퍼 + 건물 내진 면진장치** → **한 페이지** (둘 묶음)
- 모두 **시각 자료** 필수
- 어려운 내용엔 **관련 대학 전공** 명시
- 자율 진행

## 산출물 (4개 sub-page, 양 리포 동일 파일명)
| 파일 | 내용 | 묶음 |
|---|---|---|
| `know_giants.html` | 거대 관성/충격량 4종 (유조선·화물열차·항만펜더·단조프레스) | 묶음1 |
| `know_flywheel.html` | 플라이휠 — 물리(½Iω²)·기술(자기베어링/복합재)·상징(선순환) | 단독 |
| `know_ins.html` | 관성항법 — 가속도 2번 적분→위치, 자이로, 드리프트 | 단독 |
| `know_damper.html` | 질량댐퍼(공진 상쇄) + 면진장치(주기 연장·충격량) | 묶음2 |

- 파일명은 **양 리포 동일**(`know_*.html`) → 메인앱 링크가 두 곳에서 모두 작동.
  - 메인앱 자체 이름은 repo마다 다름(perf3_motion ↔ 1sem_perf_motion)이라
    sub-page→메인 복귀는 `history.back()` 사용(하드코딩 회피).
- 위치: 프로젝트 `10_app/know_*.html` → 배포 `high1/science/know_*.html`.

## 메인앱 수정
- industGrid 카드를 **클릭→해당 sub-page 이동**(앵커)으로. 8개 중 4개만 전용 페이지,
  나머지(질량댐퍼는 damper, INS는 ins…)는 묶음/단독으로 매핑:
  - 유조선·화물열차·항만펜더·단조프레스 → `know_giants.html`
  - 플라이휠 → `know_flywheel.html`
  - INS → `know_ins.html`
  - 마천루 질량댐퍼·내진 면진 → `know_damper.html`
- 카드에 "🔎 자세히 →" 표시.

## 페이지 공통 템플릿
- 헤더: 🏠 돌아가기(history.back) + 제목 + 한 줄 hook
- 섹션: ① 무슨 일이? ② 어떻게? (SVG 도해) ③ 물리 원리(KaTeX, 단위) 
  ④ 더 깊이/깜짝사실 ⑤ 🎓 관련 대학 전공 ⑥ 🔗 꼬리물기(다른 페이지 링크)
- 모바일: 44px 버튼, 반응형. KaTeX 함정 준수.
- 시각: SVG 도해(주력) + (giants) 실제 사진은 onerror 폴백 처리.

## 꼬리물기(rabbit-hole) 연결 설계
```
메인앱(관성탭) ─┬─ giants ──→ flywheel(에너지 저장) ──→ ins(적분! 이론깊이 연결)
                ├─ flywheel ─→ damper(진동·공진) ──→ giants(거대구조)
                ├─ ins ─────→ damper(센서·제어) / 이론깊이 탭(적분)
                └─ damper ──→ flywheel(회전관성) / ins
```
각 페이지 끝 "다음 이야기" 2~3개 링크로 순환.

## 대학 전공 태그(요지)
- 플라이휠: 기계공학(동역학)·전기공학(전력계통)·재료공학(고강도 복합재)
- INS: 항공우주공학(항법·제어)·전자공학(MEMS 센서)·제어공학
- 댐퍼/면진: 건축공학·토목공학(구조·내진)·기계공학(진동제어)
- giants: 조선해양공학·기계공학·토목(항만)

## 검증/배포
1. 각 HTML `node --check` (인라인 script) + KaTeX 한글-수식 스캔.
2. `cp know_*.html` → high1/science/ + 메인앱 재배포.
3. 양 리포 push.
4. app-reviewer로 물리 정확성 검토.

## 상태
- [x] 계획 작성
- [x] 4개 sub-page 작성 (know_giants/flywheel/ins/damper)
- [x] 메인앱 industGrid 링크화 (카드→sub-page, 설명 항상 표시)
- [x] 검증(node --check, KaTeX 한글스캔 OK) + app-reviewer(치명오류 없음, 사실/표현 4건 교정)
- [x] 배포 + push (사이트 dfc4d23)
- [x] CLAUDE.md/todo 갱신

## app-reviewer 교정 반영(2026-06-07)
- giants: 크녹네비스 "선회 지름 3.7km" → "방향 트는 데 지름 수 km" (반경/지름 혼동 회피)
- damper: 흔들림 "약 40%" → "최대 약 40%"; "둥둥 떠 있는" → "좌우로 미끄러지듯" (부력 오해 회피)
- ins: 자이로 "(플라이휠처럼 회전관성)" → "옛 기계식=회전관성, 요즘=빛·미세칩" (RLG/FOG/MEMS 정확화)
- flywheel: 회생제동 "F1" → "일부 경주차(KERS)"; nlink "회전관성의 사촌" 문구 완화
