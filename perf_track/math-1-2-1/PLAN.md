# PLAN — math-1-2-1 (공통수학2 논술형 수행평가 · 생활 속 수학 탐구하기)

> 상태: **제안 (라운드 1, 2026-09-12). Nick 확인 대기 → 라운드 2에서 확정.**
> 논의 원문은 `SESSION_LOG.md`, 자료 정본은 `inbox/README.md`, 풀이·변형 축 정본은 `10_prep/01_analysis.md`.

## 1. 개요

| 항목 | 내용 |
|---|---|
| 시험 | 2학기 공통수학2 논술형 수행평가 "생활 속 수학 탐구하기", **2026-09-17(목)** 수업시간 (통합과학2 수행과 같은 날, 한국사는 9/18) |
| 배점 | 15점 (소문항 배분 미확인) |
| 형식 | **예시문항 2문제 공개. 실전 = 유형 그대로, 숫자만 조정.** 손글씨 서술(풀이 과정·단위 요구) |
| 범위 | Ⅰ. 도형의 방정식 전체 — 원의 방정식·접선(점과 직선의 거리)·두 점 사이 거리·대칭이동·최단 경로 |
| 목적함수 | **숫자가 바뀐 같은 유형 2문제를 시간 안에, 감점 없는 서술로, 손으로 쓸 수 있게 한다** |
| 아이 접점 | 프린트 팩(손 연습이 본질) + 경량 앱(원리 시각화 + 변형 드릴 랜덤 생성·즉시 채점). 질문은 앱·프린트에 적고 답은 Nick이 이 세션에 넣는다 |

## 2. 판단 (근거는 `10_prep/01_analysis.md`)

1. 시험은 **파라미터만 바뀌는 템플릿**이다. 그러므로 준비의 핵심은 ① 두 문제의 논리를 "왜 그 방법이 되는가"까지 한 번 확실히 이해 ② **만점 답안 골격**(문장 순서·근거 문장)을 손에 익힘 ③ 숫자 바꾼 변형을 손으로 6~8회 풀고 자가 채점 ④ 함정 3종(대칭 순서·반지름 빼기·단위) 점검, 네 가지다.
2. 1번은 소문항이 계단식이라 개념(K1~K6)만 정확하면 된다. 위험은 **분수·근호 손계산**과 서술 누락(접선 조건 문장, 단위). 2번은 논술 한 덩어리라 **논리 순서**(대칭 → 등거리 근거 → 부등식 → 두 원 최소 거리)가 곧 점수다.
3. 아들은 2학기 중간고사(9/8~11)에서 도형의 방정식을 막 시험 본 상태라 개념은 최근 것이다. 중간고사에서 원·이동 문항을 맞았는지 알면 약점 위치를 바로 잡을 수 있다(Q1).
4. 기존 자산 활용 방침:
   - `subject_hub/공통수학2` 단원 앱(`mat2_circle`·`mat2_move`·`mat2_coordline`·관문 특강 `mat2_sp_coord`)은 **개념 복습용 딥링크**로만 쓴다. 새로 만들지 않는다.
   - 1학기 `perf2_life_math.html`의 탭 구조(개요/문제 풀이/개념/연습+체크리스트)와 공용 퀴즈 엔진의 "되풀이·복습함" 패턴을 차용한다.
   - 루트 스킬 `se-perf-study-app`은 Canvas 애니·KaTeX 중심이라 과하다. "문제 먼저 → 풀이 점진 공개" 원칙만 가져온다.
   - science-1-2-1의 `perf-essay-prep` 파이프라인(정본 md → 프린트 팩 → 경량 앱 → 게이트 → 배포)을 수학판으로 옮긴다. 차이는 **변형 문제 생성기**(숫자 파라미터를 바꿔 문제·정답·풀이를 자동 산출하고 수치 검산)가 추가되는 것.

## 3. 산출물 (제안)

| # | 산출물 | 경로 | 라운드 | 상태 |
|---|---|---|---|---|
| P1 | 예시문항 완전 분석 — 풀이(복수 방법)·검산·개념 지도·변형 축·감점 포인트·시간 배분 | `10_prep/01_analysis.md` | 1 | ✅ |
| P2 | **만점 답안 골격** — 1번 4소문항 + 2번, 빈칸 틀(숫자만 갈아끼움) + 근거 문장 뱅크 + "쓰는 순서" | `10_prep/02_answer_templates.md` | 3 | ⬜ |
| P3 | **변형 문제 은행** — 1번 4세트 + 2번 4세트 + 함정 변형 2세트(경유 순서 바꿈·끝점이 고정점), 각 문제·정답·풀이·검산 | `10_prep/03_variant_bank.md` | 3 | ⬜ |
| T1 | 변형 생성기 — 피타고라스 삼조 기반 파라미터 → 문제문·정답·풀이 자동 산출, 무차별 탐색 검산. P3와 앱 ⚡드릴이 같은 함수를 쓴다 | `10_prep/gen/variants.mjs` | 3 | ⬜ |
| P4 | 아들에게 묻는 질문 정본 — 배점·시간·계산기·답안지·선생님 힌트·중간고사 정오 | `10_prep/04_questions_for_son.md` | 3 | ⬜ |
| O1 | **프린트 팩** — 예시문항 원문 + 모범답안 / 답안 틀 카드 2장 / 변형 10세트(문제 쪽·답 쪽 분리) / 함정 체크리스트 / 당일 시간 배분 카드 / 질문지 | `90_output/print_pack.html` | 3 | ⬜ |
| O2 | **경량 앱** (§4) | `10_prep/app/perf_circle_path.html` → 배포 `high1/math/2sem_perf_circle_path.html` | 4 | ⬜ |
| O3 | 피드백 반영판 (아들 답·배점·시간 보정) | 위 파일 갱신 | 5 | ⬜ |

콘텐츠 규칙: **P1의 풀이와 변형 축 안에서만** 만든다. 범위 밖 기법(벡터·삼각함수 접선 공식 등)은 검산 코멘트로만 두고 답안 틀에는 넣지 않는다. 수학 정확성은 생성기 수치 검산 + `se-agent-app-reviewer`(🔴 0이어야 배포).

## 4. 앱 설계 (단일 HTML · 바닐라 · SVG·DOM · KaTeX 없음 · 폰 우선)

| 탭 | 내용 | 대응 |
|---|---|---|
| 📋 시작 | 시험 정보, 4일 계획 체크(localStorage), "아빠와 클로드가 궁금한 것" 질문 카드(답 입력 + [복사]) | – |
| 🧭 원리 ① 접선 실험 | SVG 좌표평면. 외부점 A를 드래그하면 두 접선·접점·접선 길이·m이 실시간. "중심~직선 거리 = r" 눈금 표시. 3-4-5 삼각형 강조 | 1번 K3·K4·K5 |
| 🧭 원리 ② 펼치기 실험 | 예시문항 2번 그대로. [x축 대칭] [y = x 대칭] 버튼을 누르면 원과 경로가 펴지는 애니메이션 → 직선 하나가 되는 순간 길이 표시. **순서 토글**(A→P→Q 대신 A→Q→P)로 함정 체험 | 2번 K7·K8·K9 |
| ✍️ 답안 틀 | 1번·2번 문장 카드(빈칸에 숫자 채우기 → 정답 대조). 근거 문장 뱅크 | P2 |
| ⚡ 변형 드릴 | [새 문제] → 생성기가 문제 출력 → **종이에 풀고** → 답 입력(1번: m 두 개·P·길이 / 2번: 최솟값) → 즉시 채점 → 풀이 점진 공개 → 되풀이·복습함. 함정 변형 섞기 토글 | P3·T1 |
| ✅ 전날 점검 | 함정 체크 6항목, 시간 배분 카드, 검산 루틴 3개 | P1 §5·§6 |

기술 규약(과학 앱과 동일): viewport meta, 고정 px 금지, 터치 44px, `@media (max-width:640px)`, breadcrumb nav(`고1 › Math`), 파일명 `2sem_perf_circle_path.html`, localStorage 키 prefix `mat2perf_`. 게이트: 인라인 `<script>` `node --check` → innerHTML `<`+알파벳 스캔 → 규약 체크 → `se-agent-app-reviewer` 🔴 0.

## 5. 라운드 계획과 Nick 확인 지점

| 라운드 | 시점 | 할 일 | Nick |
|---|---|---|---|
| 1 | 9/12 | 인테이크(inbox·README), P1 분석, 이 제안, CLAUDE.md·`mat-start`·`mat-log` | **이 제안 검토, §7 질문 답** |
| 2 | 9/12~13 | 확정 반영: PLAN 상태 ✅, 필요 시 전용 스킬 `perf-math-variant` 생성, 상위 CLAUDE.md §6·§7 갱신 | 확정 |
| 3 | 9/13 | T1 생성기 → P2·P3·P4 정본 → O1 프린트 팩 → 커밋 | **확인 1:** P2 답안 틀 훑어보기(10분) |
| 4 | 9/13~14 | O2 앱 → 게이트 → 배포 + `high1/math/index` 카드 → 두 리포 push | **확인 2:** 아들에게 프린트·링크 전달, 질문 답 회수 |
| 5 | 9/14~16 | 아들 답 반영(배점·시간·약점) → 변형 난이도 보정 → 9/16 최종 점검판 | 피드백 입력 |
| 6 | 시험 후 | 회고: 실제 문제 vs 변형 축 예측, 스킬 승격, 공개 내림 | 결정 |

## 6. 아이 일정 (총 약 2시간 — 같은 날 과학 수행이 있어 가볍게)

| 날짜 | 할 일 | 시간 |
|---|---|---|
| 9/13 (일) | 앱 🧭원리 ①② 만져 보기 → ✍️답안 틀 읽기 → **예시문항 그대로 1회 손으로** → 모범답안 대조 | 40분 |
| 9/14 (월) | 프린트 변형 1번×1·2번×1 손으로 → 답 쪽으로 자가 채점 → 틀린 자리 답안 틀 재확인 | 30분 |
| 9/15 (화) | 변형 1번×1·2번×1 + **함정 변형 1개**(순서 바꿈) | 30분 |
| 9/16 (수) | ✅전날 점검 + 변형 1세트 **시간 재며**(20+18분) | 30분 |
| 9/17 (목) | 등교 전 답안 틀 카드 2장 | 5분 |

## 7. 열린 질문 (라운드 2에서 Nick이 답할 것)

| # | 질문 | 내 권장 | 상태 |
|---|---|---|---|
| Q1 | 아들 기준 정보 — 배점 배분, 시험 시간, 계산기·자, 답안지 형태, 선생님 힌트, **중간고사에서 원·이동 문항 정오** | P4 질문지로 아들에게. 중간고사 시험지가 `exam_track`에 들어오면 그걸로 대신 확인 | ⬜ |
| Q2 | 앱(O2)을 만들지, 프린트 팩(O1)만으로 갈지 | **만든다.** 변형 랜덤 생성·즉시 채점과 "펼치기" 애니메이션은 종이로 못 하고, 이해 시간을 줄인다. 다만 경량(SVG·DOM)으로 | ⬜ |
| Q3 | 1-3 접점의 기본 방법: A(대입→중근, 교과서 표준) vs B(수선의 발, 계산 짧음) | 답안 틀은 **A 기본 + B 검산**. 아들이 B를 편해하면 바꿈 | ⬜ |
| Q4 | 2번 "A→P→Q→B 순서 성립" 보강 한 줄을 답안 틀에 넣을지 | **선택 표시**로 넣는다(시간 남으면 쓰기) | ⬜ |
| Q5 | 스킬: 이번 라운드에 `mat-start`·`mat-log` 생성. 라운드 3의 파이프라인을 `perf-math-variant` 스킬로 고정할지 / `sci-`·`soc-`·`mat-` start·log 3벌을 `perf_track/.claude/skills/perf-start`·`perf-log`로 승격할지 | 파이프라인 스킬은 **만든다**(수학 논술형이 학기마다 반복). 승격은 society 세션과 파일 충돌을 피해 **Nick이 시점 지정** | ⬜ |
| Q6 | 배포 공개(`jwj-nick.github.io/high1/math/`)와 시험 후 내림 | 과학과 같게: 공개, 내림은 라운드 6 | ⬜ |
| Q7 | 사진 배경의 **"수학 독서 탐구하기" 수행평가**(9/29·9/30)를 `math-1-2-2`로 인테이크할지 | 안내문 사진이 오면 바로 골격만 만든다 | ⬜ |

## 8. 변경 이력

| 날짜 | 내용 |
|---|---|
| 2026-09-12 | 라운드 1: inbox 2장 + README 색인, P1 분석(수치 검산 포함), 이 제안 작성. |
