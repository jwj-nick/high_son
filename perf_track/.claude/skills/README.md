# perf_track 공통 스킬 — 아직 비어 있다

**투기적 제작 금지.** 여기는 둘 이상의 과제에서 실제로 재사용됨이 확인된 스킬만 놓는 자리다. 과제 하나만을 위한 스킬은 그 과제 폴더의 `<task-slug>/.claude/skills/`에 만든다.

첫 과제가 들어오면 `../../CLAUDE.md` §8 "기존 자산" 표부터 확인한다 — 1학기 때 만든 `se_perf_study_app`(계산·분석형), `P03`(다단계형), `P04`(인물조사형) 패턴이 이미 있다.
