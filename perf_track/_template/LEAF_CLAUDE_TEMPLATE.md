# CLAUDE.md — <task name>

> 새 과제 폴더를 만들 때 이 파일을 `<task-slug>/CLAUDE.md`로 복사해서 채운다. 상위 계약은 [`../CLAUDE.md`](../CLAUDE.md).

## 과제 정의

- 과목:
- 공지일 / 마감:
- 학교가 요구하는 최종 제출 형태: (서면 에세이 / 구두 발표 / 포스터·모형 / 계산·자료해석 보고서 / 다단계 탐구 — 해당하는 것에 표시, 복수 가능)
- 채점기준(rubric) 요약:

## 준비 전략 — 이 과제에 앱이 필요한가?

상위 `CLAUDE.md` §6 매핑표에서 고른다. 애매하면 조합 가능.

- [ ] 개념/암기가 약점 → 카드·퀴즈 앱
- [ ] 서면 논리 구성이 약점 → 예상 문항 + 모범답안 대조 자료(앱 불필요)
- [ ] 발표 전달력이 약점 → 대본 카드 + 타이머 + 질의응답 연습 앱
- [ ] 계산·자료해석이 핵심 → `se_perf_study_app` 패턴 재사용
- [ ] 다단계 과정 자체가 채점 대상 → `P03` 체크포인트 패턴 재사용
- [ ] 인물·사례 조사 → `P04` 3앱 패턴 재사용
- [ ] 포스터·모형 제작 → 앱 대신 준비 체크리스트

## 이 과제에서 쓰는 스킬

공통(`perf_track/.claude/skills/`)에서 가져온 것과 이 폴더 `.claude/skills/`에만 있는 전용 스킬을 구분해서 적는다.

- 공통:
- 전용(신설 사유 한 줄):

## 구조

```
<task-slug>/
├── CLAUDE.md
├── .claude/skills/   ← 이 과제 전용 스킬 (없으면 빈 채로 둔다)
├── 00_notice/        ← 공지문·rubric 원본 (수정 금지)
├── 10_prep/          ← 조사·초안·연습 기록
└── 90_output/        ← 최종 제출 형태 그대로
```
