# 사회과 학습 앱 — 착수(Migration) 가이드

> **목적:** 고1 **통합사회1·2 + 한국사1·2** 인터랙티브 학습 앱을, 이미 완성·배포한 **통합과학1·2 / 공통수학1·2** 파이프라인을 그대로 이식해 만든다.
> **성격:** 이 문서는 *셋업될 새 워크스페이스로 들고 갈 착수 지도*다. 실제 앱 제작은 새 디렉토리에서 시작한다.
> **작성:** 2026-07-22 (기초 논의 단계). 확정 아님 — 아래 "논의 미결정" 참고.

---

## 1. 이미 검증된 것 (그대로 재사용)

`공통과목_학습허브/`(통합과학1·2, 공통수학1·2)에서 **31개 앱**으로 검증 완료:

- **단일 파일 HTML 앱** (바닐라 JS, 빌드 없음) — `sci*_.html`, `mat*_.html`
- **탭 골격**: 개요 → 개념 인터랙티브 → 세상 속으로 → 🔥퀴즈10 → ✅정리
- **8원칙 콘텐츠 블록** (SSOT: `공통과목_학습허브/CONTENT_PRINCIPLES.md`)
- **퀴즈 엔진**: Fisher-Yates 셔플 + `q.a=q.o.indexOf(correct)` 재추적, 문제·보기 `textContent`(안전), 해설 `innerHTML`
- **허브 + 상위 index 카드** 패턴 (`high1/index.html`의 "전 단원 학습 허브" 섹션)
- **검증 게이트**: 인라인 JS `node --check` + 렌더 스캔 + `se-agent-app-reviewer` (개념/사실 정확성)
- **배포**: private `high_son`(워크스페이스) → public `jwj-nick.github.io/high1/{과목}/`, 두 리포 커밋·push, 라이브 200 검증

→ **사회과는 위 골격을 100% 재사용하고, "개념 인터랙티브"만 사회과용으로 치환**한다.

---

## 2. 핵심 이식 포인트 — 인터랙션 치환

과학/수학의 Canvas 그래프를 사회과에 맞는 위젯으로 바꾼다. (전부 바닐라 SVG/Canvas/DOM, 외부 라이브러리 없음)

| 과학·수학 인터랙티브 | → 사회과 대응 위젯 |
|---|---|
| Canvas 그래프(포물선·원·함수) | 🕰️ **연표(타임라인)** — 가로 스크롤, 시대/사건 클릭 → 상세 |
| PhET 임베드 | 🗺️ **지도** — SVG 지도 또는 이미지 crop + 클릭 핫스팟 (삼국·조선8도·문화권·분쟁지역·기후대) |
| 슬라이더 파라미터 | 📈 **통계 시각화** — 인구·도시화율·경제지표 막대/선 (AM-GM 막대 패턴 재사용) |
| 판별식 3케이스 토글 | 🔀 **인과 다이어그램** — DOM 박스+화살표 (배경→전개→결과), 단계 공개 |
| 곱셈공식/역이대우 카드 | 🃏 **개념·인물·용어 카드 뒤집기** (mat2_logic 카드 패턴 그대로) |
| 행렬 곱 하이라이트 | ⚖️ **비교표 인터랙션** — 사상·제도·시대 비교, 항목 클릭 강조 |
| — (신규) | 🔢 **사건 순서 맞추기** — 정렬형 퀴즈 (연대 감각) |

**KaTeX는 제거** (사회과는 수식 없음) → head가 더 단순해짐. (통계 수식이 필요하면 최소한만.)

---

## 3. 8원칙 → 사회과 매핑

CONTENT_PRINCIPLES.md 그대로, 시그니처만 사회과 언어로:

- 💥 **훅** — "왜 배우나" (현재 이슈와 연결: 기후난민·다문화·인권·통일)
- 🏛️ **인물/사건** (과학자 블록 대응) — 역사가·사상가·핵심 사건
- 🧩 **개념 인터랙티브** — 연표·지도·다이어그램(§2)
- 📌 **평생기억** — 어른 상식으로 남길 3가지 (연대·개념·의의)
- 💡 **인사이트** — 사건의 구조·반복되는 패턴(원인→결과)
- 🏠 **실생활** — 헌법·선거·시장·다문화 등 생활 접점
- 🏭🎓 **산업·진로** — 법조·외교·언론·행정·지리정보(GIS)·문화기획·역사연구
- 🔥 **퀴즈10** + 사건 순서 맞추기
- ✅ **정리** — 비교표 + 자가체크 + 약점태그 + 허브 순환링크

---

## 4. 단원 지도 (잠정 · 2022 개정 · 교과서로 최종 확정)

> ⚠️ 아래는 표준 커리큘럼 기준 **잠정안**. 아들 실제 교과서/진도표로 단원명·순서·개수 확정 후 착수.

### 통합사회1 (1학기) — prefix `soc1_`
- Ⅰ. 통합적 관점 (인간·사회·환경을 보는 눈)
- Ⅱ. 인간, 사회, 환경과 행복
- Ⅲ. 자연환경과 인간 (기후·지형, 자연재해, 환경문제, 생태시민)
- Ⅳ. 문화와 다양성 (문화권, 문화 변동, 다문화, 문화 상대주의)

### 통합사회2 (2학기) — prefix `soc2_`
- Ⅴ. 인권 보장과 헌법
- Ⅵ. 사회 정의와 불평등
- Ⅶ. 시장경제와 지속가능발전
- Ⅷ. 세계화와 평화
- Ⅸ. 미래와 지속가능한 삶

### 한국사1 (전근대) — prefix `kh1_`
- Ⅰ. 우리 역사의 형성과 고대 국가 (선사~남북국)
- Ⅱ. 고려의 성립과 변천
- Ⅲ. 조선의 성립과 발전
- Ⅳ. 조선 사회의 변동 (조선 후기)

### 한국사2 (근현대) — prefix `kh2_`
- Ⅰ. 근대 국가 수립 운동 (개항~대한제국)
- Ⅱ. 일제의 식민 지배와 민족 운동
- Ⅲ. 대한민국의 발전과 현대 세계 (해방~현재)

**대략 규모:** 통합사회 4+5 + 한국사 4+3 = **~16 단원앱 + 허브 4개**. (과학 18 + 수학 13 = 31앱과 비슷한 볼륨)

---

## 5. 워크스페이스 셋업 (권장)

```
70_HighSchool/사회과_학습허브/              ← 새 워크스페이스 (공통과목_학습허브 미러)
├── CONTENT_PRINCIPLES.md                  ← 심볼릭/복사 (또는 공통것 참조)
├── 통합사회1/{PLAN.md, 10_app/, 02_text/}
├── 통합사회2/{PLAN.md, 10_app/, 02_text/}
├── 한국사1/{PLAN.md, 10_app/, 02_text/}
└── 한국사2/{PLAN.md, 10_app/, 02_text/}
```

- 이 폴더에서 Claude 실행 (자기완결). PLAN.md가 과목별 단원 지도·진행상태 SSOT.
- 템플릿: `공통과목_학습허브/통합과학2/10_app/sci2_climate.html` 또는 `공통수학2/10_app/mat2_logic.html`를 스캐폴드로 복사 → KaTeX 헤더 제거 → 인터랙티브만 교체.

---

## 6. 배포 타깃

| 과목 | 배포 폴더 | 허브 파일 | 주의 |
|---|---|---|---|
| 통합사회1·2 | `high1/society/` | `soc1_hub.html`, `soc2_hub.html` | 기존 메가시티 수행평가와 형제 |
| 한국사1·2 | `high1/history/` | `kh1_hub.html`, `kh2_hub.html` | 기존 정도전 수행평가와 형제. **루트 `/history`(세계사 사이트)와 다름** |

- **고1 상위 index** (`high1/index.html`)의 "전 단원 학습 허브" 섹션을 **4카드 → 8카드**로 확장(통합사회1·2·한국사1·2 추가).
- 각 과목 섹션 index(`society/index.html`, `history/index.html`)에도 허브 카드 추가(수학 index 패턴 그대로).
- 배포는 public `jwj-nick.github.io` — **동시 작업 세션 있으므로 `git pull --rebase` 먼저**.

---

## 7. 검증 규율 (사회과 특화)

1. **인라인 JS** `node --check` (기존 스캐너 재사용: `scan_mat2.py`에서 KaTeX 체크만 빼면 됨)
2. **`<letter` innerHTML 잘림** 스캔 — 퀴즈 해설에서 `<`+문자는 태그로 파싱돼 잘림 → `&lt;` 또는 앞뒤 공백. (과학/수학에서 겪은 반복 버그)
3. ⭐ **사실 정확성 = 최우선** — 연대·왕조·조약·인물·제도. `se-agent-app-reviewer`로 **팩트 검증**(과학 정확성 검증의 사회판). 특히 한국사 연대·순서.
4. **저작권** — 교과서/사료 스캔 배포 금지. 지도·도판은 **직접 제작(SVG)** 또는 공개 도메인만. (김치 앱 저작권 규율 계승)

---

## 8. 착수 순서 (per-단원, 과학/수학과 동일)

1. 교과서/커리큘럼으로 단원 스코프 확정 → PLAN.md 갱신
2. 스캐폴드 복사 → 콘텐츠(8원칙) + 사회과 인터랙티브 작성
3. `node --check` + `<letter` 스캔
4. 허브 카드 연결
5. `se-agent-app-reviewer` **팩트+8원칙** 검토 → 🔴/🟡 반영
6. 배포(society/history 복사 → 두 리포 커밋·push) → 라이브 200 검증
7. 1학기 것(통합사회1·한국사1) 먼저 → 2학기(통합사회2·한국사2)

---

## 9. 논의 미결정 (착수 전 결정)

1. **워크스페이스 구조**: `사회과_학습허브` 하나로 4과목 통합 vs 통합사회/한국사 분리 vs 과목별 완전 분리
2. **핵심 인터랙션 우선순위**: 연표 / 지도 / 인과 다이어그램 중 무엇을 사회과 대표 위젯으로
3. **한국사 앱 ↔ 기존 자산 관계**: 정도전 수행평가·루트 세계사 사이트(`/history`)와 **중복 회피 + 크로스링크** 전략
4. **범위/우선순위**: 4과목 전부 vs 1학기(통합사회1·한국사1) 먼저
5. **교과서 확보 여부**: 실제 단원명·진도 확정 시점 (없으면 표준안으로 잠정 착수)
